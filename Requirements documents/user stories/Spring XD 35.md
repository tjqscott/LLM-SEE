As a developer, I'd like to port {{file}} module from XD to s-c-s repo, so I can use it as {{source}} module to build streaming pipeline.
As a s-c-d developer, I'd like to break the build lifecycle to bundle SPI deployers individually, so I don't have to build {{admin}} with all the deployer variations as one whole thing.
As a developer, I'd like to get rid off {{XDRuntimeException}} from XD.
As a user, I'd like to use the latest release of {{gemfire}} sink, so I can create a streaming pipeline to land data in gemfire.
As a developer, I'd like to resolve remaining gaps wrt CI pipelines for Data Flow and the family, so I can continuously evaluate functionalities on every commit.
As a Flo user, I'd like to have {{timeout}} and {{pollInterval}} as global options at the DSL level, so I can override the defaults at will.
As a developer, I'd like to upgrade to SI 4.2.1 release, so I can take advantage of the latest improvements.
As a developer, I'd like to review and refactor {{JobLaunchingTasklet}}, so I can improve performance characteristics.
As a developer, I'd like to enhance test coverage to capture DSL and XML generation variants.
As a user, I'd like to use SpEL expressions inline at the stream definition level, so I can operate on the payload consistently while using any OOTB, including the custom modules.
