As a developer, I need the ability to create windows and present  them onto a scene so that they can be positioned, sized, and drawn  onto the display.
As a BlackBerry developer, I want the ability to integrate with  other applications on the platform in order to provide better  multitasking for the user.
As a developer, I need the ability to send form-encoded POST data in a request.
As a developer, I want the ability to mark a textfield  containing sensitive information (ex: password) so that it is  masked to hide previously typed characters.
As Android 2.3.2 and earlier has dropped into single digits long ago, TiMob 3.1.1 will drop it for testing, performance, and other reasons. The code should reflect this, if nothing else, in making sure the manifest's api version moves to 10, and that the CLI does not override this and pull it down to 8.
as an option it should be possible to compile an app an precache all Ti.includes to speedup application
As a developer, I want a view that provides a viewport of a content  view that can be scrolled using gestures both horizontally and vertically.    The existing Titanium API for this feature is Ti.UI.ScrollView.  Cascades does provide a control (ScrollView) which should meet  the requirements for this view.
