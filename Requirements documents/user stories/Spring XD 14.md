As a developer, I'd like to create persistent repository for streams, so I could leverage the persisted metadata and reestablish the streaming pipe under failure conditions.
As a developer, I'd like to add documentation on escape quotes, so when someone using Sqoop job can double escape {{\\\\N}} instead of sending quotes {{'\N'}} to successfully submit the job.
As a Spring XD user, I'd like to create streaming pipelines, so I can take advantage of latest specs from both XD and Spark/Spark Streaming.
As a developer, I'd like to document how to nest batch jobs and workflows in XD, so it will be easy for end-users to use it as reference.
As a user, I'd like to parameterize all Import Options, so I can eliminate the need for {{—args}} option since it gets confusing.
As a user, I'd like to parameterize CodeGen Options, so I can generate code on the fly as needed.
As a user, I'd like to parameterize Merge Options, so I can incrementally consume the delta with the help of megastore.
As a user, I'd like to have the ability to use expressions, so I can dynamically name directories/files based on the timestamp or other intermediate data point.
As a developer, I'd like to refactor the programmatic means by which the MessageBus transforms the Message so throughput performance can be optimized.
As a developer, I'd like revisit the design to determine the necessity for _ID_ and _TimeStamp_ attributes in {{Tuple}}, so I can refactor in order to improve performance throughput.
