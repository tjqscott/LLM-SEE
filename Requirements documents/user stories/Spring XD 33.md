As a s-c-d developer, I'd like to explore options to bootstrap and setup Lattice based infrastructure for s-c-d's bare metal deployment.
As a spring-cloud-stream user, I'd like to build stream definitions using dot-delimited syntax for resolving properties for Tuple and JSON.
As an s-c-d user, I'd like to have tab completion on shell, so I can interact with the modules and its available options.
As an s-c-d user, I'd like to deploy s-c-d on Mesos.
As an s-c-d developer, I'd like to move redis {{@Rule}} to a separate repo, so I can consume the test fixtures in different projects.
As an s-c-d developer, I'd like to move kafka {{@Rule}} to a separate repo, so I can consume the test fixtures in different projects.
As an s-c-d developer, I'd like to move rabbit {{@Rule}} to a separate repo, so I can consume the test fixtures in different projects.
As an s-c-d user, I'd like to take advantage of admin running on variety of platforms such as Lattice, YARN or CF. I'd like to access REST APIs consistently across these platforms.
As a SCDF user, I want to be able to register artifacts as libraries, so that I can reference them in include and exclude statements.
As an s-c-d user, I'd like to have the option to use _named channels_, so I can create streaming pipelines without source or sink modules.
